---
phase: 10-world-rendering-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client\Graphics\Rendering\WorldRenderer.cs, OpenTibia.Client\Input\InputManager.cs, OpenTibia.Client\ClientServer.cs, OpenTibia.Client\Program.cs]
autonomous: true
requirements: [RENDER-01, RENDER-02, INPUT-01]

must_haves:
  truths:
    - "WorldRenderer correctly iterates over LocalGameState tiles and renders them."
    - "InputManager captures arrow keys and dispatches Walk commands."
  artifacts:
    - path: "OpenTibia.Client/Graphics/Rendering/WorldRenderer.cs"
      provides: "Tile-based world rendering"
    - path: "OpenTibia.Client/Input/InputManager.cs"
      provides: "Keyboard input handling"
---

<objective>
Implement and integrate world rendering and input handling.

Purpose: Finalize the client prototype by enabling players to see the game world and interact via keyboard.
Output: Functional rendering loop and input processing in the main client project.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Client/LocalGameState.cs
@OpenTibia.Client/Graphics/SpriteLoader.cs
@OpenTibia.Client/ClientServer.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WorldRenderer</name>
  <files>OpenTibia.Client/Graphics/Rendering/WorldRenderer.cs</files>
  <action>
    Create `OpenTibia.Client/Graphics/Rendering/WorldRenderer.cs` to handle tile-based rendering of ground, items, and creatures using Raylib and SpriteLoader.
  </action>
  <verify>
    <automated>ls OpenTibia.Client/Graphics/Rendering/WorldRenderer.cs</automated>
  </verify>
  <done>WorldRenderer implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement InputManager</name>
  <files>OpenTibia.Client/Input/InputManager.cs</files>
  <action>
    Create `OpenTibia.Client/Input/InputManager.cs` to capture keyboard arrow keys and call the `Walk` method on `ClientServer`.
  </action>
  <verify>
    <automated>ls OpenTibia.Client/Input/InputManager.cs</automated>
  </verify>
  <done>InputManager implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Update ClientServer</name>
  <files>OpenTibia.Client/ClientServer.cs</files>
  <action>
    Add a `Walk(Direction direction)` stub to `ClientServer` to receive movement requests from `InputManager`.
  </action>
  <verify>
    <automated>grep "Walk(Direction direction)" OpenTibia.Client/ClientServer.cs</automated>
  </verify>
  <done>ClientServer updated with Walk stub.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate in Program.cs</name>
  <files>OpenTibia.Client/Program.cs</files>
  <action>
    Update the main game loop in `Program.cs` to initialize `WorldRenderer` and `InputManager` and call their `Render` and `Update` methods every frame.
  </action>
  <verify>
    <automated>dotnet build OpenTibia.Client/OpenTibia.Client.csproj</automated>
  </verify>
  <done>Components integrated and project builds.</done>
</task>

</tasks>

<verification>
Project builds successfully. Visual and logic components are correctly instantiated and called in the main loop.
</verification>

<success_criteria>
- Project compiles without errors.
- WorldRenderer and InputManager are integrated.
- Movement commands are logged to console (via stub).
</success_criteria>

<output>
After completion, create `.planning/phases/10-world-rendering-input/10-01-SUMMARY.md`
</output>
