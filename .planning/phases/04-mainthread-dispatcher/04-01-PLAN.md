---
phase: 04-mainthread-dispatcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Threading/MainThreadDispatcher.cs, OpenTibia.Client/Program.cs, OpenTibia.Client.Tests/SpriteLoaderTests.cs]
autonomous: true
requirements: [CLN-02]

must_haves:
  truths:
    - "MainThreadDispatcher exists and inherits from Dispatcher."
    - "MainThreadDispatcher does not start its own worker thread."
    - "The client game loop calls ExecuteAll() on the main thread dispatcher."
  artifacts:
    - path: "OpenTibia.Threading/MainThreadDispatcher.cs"
      provides: "Specialized Dispatcher for main-thread execution"
---

<objective>
Implement the MainThreadDispatcher and integrate it into the client's main loop.

Purpose: Allow Raylib-specific operations (like texture creation) to be scheduled from background threads but executed on the main thread.
Output: MainThreadDispatcher class and updated Program.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Threading/Dispatcher.cs
@OpenTibia.Client/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MainThreadDispatcher</name>
  <files>OpenTibia.Threading/MainThreadDispatcher.cs</files>
  <action>
    Create `OpenTibia.Threading/MainThreadDispatcher.cs`.
    It should inherit from `Dispatcher` and call `base(false)` in its constructor.
  </action>
  <verify>
    <automated>ls OpenTibia.Threading/MainThreadDispatcher.cs</automated>
  </verify>
  <done>MainThreadDispatcher implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into Program.cs</name>
  <files>OpenTibia.Client/Program.cs</files>
  <action>
    In `OpenTibia.Client/Program.cs`:
    1. Instantiate `MainThreadDispatcher mainDispatcher = new MainThreadDispatcher();`.
    2. Pass `mainDispatcher` as the first argument to `ClientServer` constructor.
    3. In the `while (!Raylib.WindowShouldClose())` loop, call `mainDispatcher.ExecuteAll();` at the beginning of the `Update` phase.
  </action>
  <verify>
    <automated>grep "mainDispatcher.ExecuteAll()" OpenTibia.Client/Program.cs</automated>
  </verify>
  <done>MainThreadDispatcher integrated into the game loop.</done>
</task>

<task type="auto">
  <name>Task 3: Restore SpriteLoaderTests</name>
  <files>OpenTibia.Client.Tests/SpriteLoaderTests.cs</files>
  <action>
    Revert the temporary change in `OpenTibia.Client.Tests/SpriteLoaderTests.cs` to use `MainThreadDispatcher` instead of `Dispatcher`.
  </action>
  <verify>
    <automated>grep "new MainThreadDispatcher()" OpenTibia.Client.Tests/SpriteLoaderTests.cs</automated>
  </verify>
  <done>Tests updated to use the correct specialized dispatcher.</done>
</task>

</tasks>

<verification>
Build the solution and run tests.
</verification>

<success_criteria>
- Project compiles.
- MainThreadDispatcher is used in the main loop.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mainthread-dispatcher/04-01-SUMMARY.md`
</output>
