---
phase: 11-architectural-polish-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client/Commands/Incoming/SelfAppearCommand.cs, OpenTibia.Client/Commands/Incoming/MapDescriptionCommand.cs, OpenTibia.Client/Commands/Incoming/CreatureMoveCommand.cs, OpenTibia.Game.Common/Common/IServer.cs, OpenTibia.Client/Network/ClientPacketReader.cs, OpenTibia.Game.Common/Common/IClientServer.cs, OpenTibia.Client/Input/InputManager.cs, OpenTibia.Client/LocalGameState.cs, OpenTibia.Client/OpenTibia.Client.csproj, OpenTibia.Client/Network/IncomingCommandHandler.cs, OpenTibia.Client/Program.cs]
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06, FIX-07]

must_haves:
  truths:
    - "All 7 architectural fixes are applied correctly."
    - "Project builds successfully after changes."
  artifacts:
    - path: "OpenTibia.Client/Network/IncomingCommandHandler.cs"
      provides: "Unified packet registration source of truth"
---

<objective>
Execute the 7 architectural fixes requested to polish the prototype.

Purpose: Improve decoupling, resolve compilation errors, and unify the registration system.
Output: Refactored and corrected project files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Context.Current (FIX-01)</name>
  <files>
    - OpenTibia.Client/Commands/Incoming/SelfAppearCommand.cs
    - OpenTibia.Client/Commands/Incoming/MapDescriptionCommand.cs
    - OpenTibia.Client/Commands/Incoming/CreatureMoveCommand.cs
  </files>
  <action>Replace `Context as ClientContext` with `Context.Current as ClientContext` in the 3 specified files.</action>
  <verify>
    <automated>grep "Context.Current as ClientContext" OpenTibia.Client/Commands/Incoming/SelfAppearCommand.cs</automated>
  </verify>
  <done>Context.Current fixed in commands.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup IServer (FIX-02)</name>
  <files>OpenTibia.Game.Common/Common/IServer.cs</files>
  <action>Remove redundant ServerName, ServerVersion, and Status property declarations.</action>
  <verify>
    <automated>! grep "ServerName" OpenTibia.Game.Common/Common/IServer.cs</automated>
  </verify>
  <done>IServer redundancy removed.</done>
</task>

<task type="auto">
  <name>Task 3: Add Packet Logging (FIX-03)</name>
  <files>OpenTibia.Client/Network/ClientPacketReader.cs</files>
  <action>Add error logging for unknown packet IDs in the `Read()` method.</action>
  <verify>
    <automated>grep "Unknown packet ID" OpenTibia.Client/Network/ClientPacketReader.cs</automated>
  </verify>
  <done>Packet logging added.</done>
</task>

<task type="auto">
  <name>Task 4: Decouple InputManager (FIX-04)</name>
  <files>
    - OpenTibia.Game.Common/Common/IClientServer.cs
    - OpenTibia.Client/Input/InputManager.cs
  </files>
  <action>
    - Add `Walk(Direction)` to `IClientServer`.
    - Update `InputManager` to use `IClientServer` instead of concrete `ClientServer`.
  </action>
  <verify>
    <automated>grep "Walk(Direction direction)" OpenTibia.Game.Common/Common/IClientServer.cs</automated>
  </verify>
  <done>InputManager decoupled.</done>
</task>

<task type="auto">
  <name>Task 5: Cleanup LocalGameState (FIX-05)</name>
  <files>OpenTibia.Client/LocalGameState.cs</files>
  <action>Remove the explicit constructor that creates a default tile.</action>
  <verify>
    <automated>! grep "public LocalGameState()" OpenTibia.Client/LocalGameState.cs</automated>
  </verify>
  <done>LocalGameState constructor cleaned up.</done>
</task>

<task type="auto">
  <name>Task 6: Cleanup Dependencies (FIX-06)</name>
  <files>OpenTibia.Client/OpenTibia.Client.csproj</files>
  <action>Remove the project reference to `OpenTibia.Game`.</action>
  <verify>
    <automated>! grep "OpenTibia.Game.csproj" OpenTibia.Client/OpenTibia.Client.csproj</automated>
  </verify>
  <done>OpenTibia.Game reference removed.</done>
</task>

<task type="auto">
  <name>Task 7: Unified Registration (FIX-07)</name>
  <files>
    - OpenTibia.Client/Network/IncomingCommandHandler.cs
    - OpenTibia.Client/Program.cs
  </files>
  <action>
    - Refactor `IncomingCommandHandler` to auto-register packet IDs with `ClientPacketReader`.
    - Remove manual registrations from `Program.cs`.
  </action>
  <verify>
    <automated>grep "Register<SelfAppearIncomingPacket" OpenTibia.Client/Network/IncomingCommandHandler.cs</automated>
  </verify>
  <done>Registration system unified.</done>
</task>

</tasks>

<verification>
Run `dotnet build OpenTibia.Client/OpenTibia.Client.csproj` to ensure all fixes compile and correctly interact.
</verification>

<success_criteria>
- All 7 tasks completed as specified.
- OpenTibia.Client project builds successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/11-architectural-polish-fixes/11-01-SUMMARY.md`
</output>
