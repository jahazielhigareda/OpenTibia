---
phase: 03-clientserver-refactor
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [OpenTibia.Client/ClientServer.cs]
autonomous: true
requirements: [CLN-01]

must_haves:
  truths:
    - "ClientServer implements IClientServer."
    - "ClientServer no longer implements IServer."
    - "Unnecessary backend-only properties and methods are removed from ClientServer."
  artifacts:
    - path: "OpenTibia.Client/ClientServer.cs"
      provides: "Refactored ClientServer implementation"
---

<objective>
Refactor ClientServer to implement the new IClientServer interface and remove redundant backend-only code.

Purpose: Reduce implementation surface of ClientServer and focus only on client-side requirements.
Output: Simplified ClientServer.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-clientserver-refactor/03-01-SUMMARY.md
@OpenTibia.Game.Common/Common/IClientServer.cs
@OpenTibia.Client/ClientServer.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ClientServer to implement IClientServer</name>
  <files>OpenTibia.Client/ClientServer.cs</files>
  <action>
    Modify `ClientServer` in `OpenTibia.Client/ClientServer.cs` to implement `IClientServer` instead of `IServer`.
    Keep the core implementation of:
    - `ServerName`, `ServerVersion`, `Status`.
    - `GameState`, `_mainDispatcher`, `_backgroundDispatcher`.
    - `Connect`, `Start`, `Stop`, `Pause`, `Continue`, `Post`, `QueueForExecution`.
    - `Dispose`.

    Ensure that `Post` and `QueueForExecution` implementations are updated to correctly use `IClientServer` and the new `ClientContext` constructor.
  </action>
  <verify>
    <automated>grep "public class ClientServer : IClientServer" OpenTibia.Client/ClientServer.cs</automated>
  </verify>
  <done>ClientServer refactored to implement the correct interface.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup IServer properties</name>
  <files>OpenTibia.Client/ClientServer.cs</files>
  <action>
    Remove all properties and methods from `ClientServer.cs` that were required only by `IServer` and are now redundant.
    This includes: `MessageCollectionFactory`, `ClientFactory`, `DatabaseFactory`, `Statistics`, `Logger`, `PathResolver`, `Randomization`, `Clock`, `RateLimiting`, `WaitingList`, `Channels`, `RuleViolations`, `Guilds`, `Parties`, `Tradings`, `NpcTradings`, `Combats`, `GameObjectPool`, `GameObjects`, `GameObjectComponents`, `GameObjectEventHandlers`, `EventHandlers`, `PositionalEventHandlers`, `LuaScripts`, `PluginLoader`, `Values`, `Config`, `Features`, `Quests`, `Outfits`, `Mounts`, `Vocations`, `Plugins`, `Scripts`, `GameObjectScripts`, `ItemFactory`, `PlayerFactory`, `MonsterFactory`, `NpcFactory`, `Map`, `Spawns`, `Raids`, `Pathfinding`.
    Also remove empty implementations like `ReloadPlugins`, `KickAll`, `Save`, `Clean`.
  </action>
  <verify>
    <automated>! grep "public IMap Map" OpenTibia.Client/ClientServer.cs</automated>
  </verify>
  <done>Redundant IServer boilerplate removed.</done>
</task>

</tasks>

<verification>
Run dotnet build on OpenTibia.Client to ensure all references to properties were removed correctly.
</verification>

<success_criteria>
- ClientServer implements IClientServer.
- No remaining IServer baggage.
- OpenTibia.Client compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-clientserver-refactor/03-02-SUMMARY.md`
</output>
