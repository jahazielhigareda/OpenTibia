---
phase: 03-clientserver-refactor
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [OpenTibia.Client.Tests/ClientContextTests.cs, OpenTibia.Client.Tests/CommandPipelineTests.cs]
autonomous: false
requirements: [CLN-01]

must_haves:
  truths:
    - "Unit tests pass with the new IClientServer interface."
    - "The client still renders sprites in the interactive loop."
  artifacts:
    - path: "OpenTibia.Client.Tests/ClientContextTests.cs"
      provides: "Updated unit tests for ClientContext"
    - path: "OpenTibia.Client.Tests/CommandPipelineTests.cs"
      provides: "Updated unit tests for CommandPipeline"
---

<objective>
Update client tests to use the new interface and perform final verification.

Purpose: Ensure that the refactoring did not break existing functionality or tests.
Output: Updated tests and passing build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-clientserver-refactor/03-02-SUMMARY.md
@OpenTibia.Client.Tests/ClientContextTests.cs
@OpenTibia.Client.Tests/CommandPipelineTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update client tests to use IClientServer</name>
  <files>OpenTibia.Client.Tests/ClientContextTests.cs, OpenTibia.Client.Tests/CommandPipelineTests.cs</files>
  <action>
    Review `OpenTibia.Client.Tests/ClientContextTests.cs` and `OpenTibia.Client.Tests/CommandPipelineTests.cs`.
    Any instantiation of `ClientContext` or mocking of its server must now use `IClientServer` instead of `IServer`.
    Update variable types and mock setups as necessary.
  </action>
  <verify>
    <automated>dotnet build OpenTibia.Client.Tests</automated>
  </verify>
  <done>Client tests updated to match the new interface.</done>
</task>

<task type="auto">
  <name>Task 2: Run all client tests</name>
  <files>None</files>
  <action>
    Execute all unit tests in `OpenTibia.Client.Tests` and ensure they pass.
  </action>
  <verify>
    <automated>dotnet test OpenTibia.Client.Tests</automated>
  </verify>
  <done>Client tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refactored ClientServer and ClientContext</what-built>
  <how-to-verify>
    1. Run the Raylib client using `dotnet run --project OpenTibia.Client`.
    2. Press the `L` key to trigger sprite loading.
    3. Verify that the sprite loads correctly and is rendered (look for a character sprite in the center of the grid).
    4. Ensure no crashes occur.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Check that all unit tests pass and visual check is confirmed.
</verification>

<success_criteria>
- Tests pass.
- Application maintains original functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-clientserver-refactor/03-03-SUMMARY.md`
</output>
