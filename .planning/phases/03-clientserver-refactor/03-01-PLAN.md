---
phase: 03-clientserver-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Game.Common/Common/IClientServer.cs, OpenTibia.Game.Common/Common/IServer.cs, OpenTibia.Game.Common/Common/Context.cs, OpenTibia.Client/ClientContext.cs]
autonomous: true
requirements: [CLN-01]

must_haves:
  truths:
    - "IClientServer interface exists with the specified contract."
    - "IServer inherits from IClientServer."
    - "Context.cs supports IClientServer and handles Flush() safely for non-IServer implementations."
  artifacts:
    - path: "OpenTibia.Game.Common/Common/IClientServer.cs"
      provides: "Common interface for both Client and Server"
    - path: "OpenTibia.Game.Common/Common/Context.cs"
      provides: "Base context support for IClientServer"
---

<objective>
Define the IClientServer interface and adapt the base Context class to support it, ensuring backward compatibility for the backend.

Purpose: Decouple the client from backend-only server requirements and establish a shared contract.
Output: New IClientServer interface and updated Context/ClientContext classes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Game.Common/Common/IServer.cs
@OpenTibia.Game.Common/Common/Context.cs
@OpenTibia.Client/ClientContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IClientServer interface</name>
  <files>OpenTibia.Game.Common/Common/IClientServer.cs</files>
  <action>
    Create a new interface `IClientServer` in `OpenTibia.Game.Common` namespace (per D-10).
    It must inherit from `IDisposable`.
    Properties:
    - `string ServerName { get; }`
    - `Version ServerVersion { get; }`
    - `ServerStatus Status { get; }`
    - `ICommandHandlerCollection CommandHandlers { get; }`
    Methods:
    - `void Post(Context previousContext, Action run)`
    - `Promise QueueForExecution(Func<Promise> run)`
    - `void Start()`
    - `void Stop()`
    - `void Pause()`
    - `void Continue()`

    Use appropriate namespaces: `System`, `OpenTibia.Threading`, `OpenTibia.Game.Common.ServerObjects`.
  </action>
  <verify>
    <automated>ls OpenTibia.Game.Common/Common/IClientServer.cs</automated>
  </verify>
  <done>IClientServer.cs created with specified members.</done>
</task>

<task type="auto">
  <name>Task 2: Update IServer to inherit from IClientServer</name>
  <files>OpenTibia.Game.Common/Common/IServer.cs</files>
  <action>
    Modify `IServer` in `OpenTibia.Game.Common/Common/IServer.cs` to inherit from `IClientServer`.
    Since `IClientServer` already inherits from `IDisposable`, you can keep or remove `IDisposable` from `IServer` inheritance list.
    Ensure that overlapping members are consistent (e.g., `Post`, `QueueForExecution`, `Start`, `Stop`, etc.).
  </action>
  <verify>
    <automated>grep "interface IServer : IClientServer" OpenTibia.Game.Common/Common/IServer.cs</automated>
  </verify>
  <done>IServer now inherits from IClientServer.</done>
</task>

<task type="auto">
  <name>Task 3: Update Context to support IClientServer</name>
  <files>OpenTibia.Game.Common/Common/Context.cs</files>
  <action>
    1. Change the private field `private IServer server;` to `private IClientServer server;`.
    2. Update constructor `public Context(IServer server, Context previousContext)` to `public Context(IClientServer server, Context previousContext)`.
    3. Update `Server` property to return `server as IServer`. This preserves backend compatibility but might return null if used incorrectly in client context.
    4. Add a new property `public IClientServer ClientServer => server;`.
    5. In `AddPacket` and `Flush()` methods, wrap logic that depends on `IServer` specific members in `if (server is IServer fullServer)` blocks.
       Specifically, `Flush()` uses `EventHandlers`, `GameObjectEventHandlers`, `PositionalEventHandlers`, `Logger`, `MessageCollectionFactory`, and `Features`.
       `AddPacket` uses `MessageCollectionFactory` and `Features`.
  </action>
  <verify>
    <automated>dotnet build OpenTibia.Game.Common</automated>
  </verify>
  <done>Context.cs modified to support IClientServer safely.</done>
</task>

<task type="auto">
  <name>Task 4: Update ClientContext constructor</name>
  <files>OpenTibia.Client/ClientContext.cs</files>
  <action>
    Update `ClientContext` constructor in `OpenTibia.Client/ClientContext.cs` to accept `IClientServer` instead of `IServer`.
    The base call `base(server, previousContext)` should still work after Task 3.
  </action>
  <verify>
    <automated>grep "public ClientContext(IClientServer server" OpenTibia.Client/ClientContext.cs</automated>
  </verify>
  <done>ClientContext constructor updated to use IClientServer.</done>
</task>

</tasks>

<verification>
Check that OpenTibia.Game.Common and OpenTibia.Client compile.
</verification>

<success_criteria>
- IClientServer exists.
- Context.cs handles both IClientServer and full IServer.
- Project compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-clientserver-refactor/03-01-SUMMARY.md`
</output>
