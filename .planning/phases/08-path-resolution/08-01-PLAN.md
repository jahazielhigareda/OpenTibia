---
phase: 08-path-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client/Program.cs]
autonomous: true
requirements: [CLN-06]

must_haves:
  truths:
    - "Program.cs checks for OT_SPRITE_PATH, OT_DAT_PATH, and OT_OTB_PATH."
    - "Default paths are maintained if environment variables are not set."
  artifacts:
    - path: "OpenTibia.Client/Program.cs"
      provides: "Flexible asset path resolution"
---

<objective>
Enable flexible project configuration by using environment variables for asset paths in Program.cs.

Purpose: Allow developers to override asset locations without modifying code.
Output: Updated Program.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Client/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Program.cs asset paths</name>
  <files>OpenTibia.Client/Program.cs</files>
  <action>
    In `OpenTibia.Client/Program.cs`:
    1. Replace hardcoded paths with `Environment.GetEnvironmentVariable("OT_SPRITE_PATH") ?? "OpenTibia.Client/data/things/860/Tibia.spr"`.
    2. Do the same for DAT and OTB paths using `OT_DAT_PATH` and `OT_OTB_PATH`.
  </action>
  <verify>
    <automated>grep "Environment.GetEnvironmentVariable" OpenTibia.Client/Program.cs</automated>
  </verify>
  <done>Path resolution updated.</done>
</task>

</tasks>

<verification>
Build OpenTibia.Client.
</verification>

<success_criteria>
- Project compiles.
- Environment variable checks are present in code.
</success_criteria>

<output>
After completion, create `.planning/phases/08-path-resolution/08-01-SUMMARY.md`
</output>
