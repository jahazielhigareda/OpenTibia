---
phase: 09-network-command-handlers
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [OpenTibia.Client/Network/IncomingCommandHandler.cs, OpenTibia.Client/ClientServer.cs]
autonomous: true
requirements: [NET-02]

must_haves:
  truths:
    - "IncomingCommandHandler maps packets to commands."
    - "ClientServer uses the handler to dispatch commands on receipt of network payloads."
  artifacts:
    - path: "OpenTibia.Client/Network/IncomingCommandHandler.cs"
      provides: "Registry for packet-to-command mapping"
---

<objective>
Wire the network connection to the command system by implementing an IncomingCommandHandler.

Purpose: Automate the dispatch of commands when packets are received over the network.
Output: IncomingCommandHandler.cs and updated ClientServer.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Client/ClientServer.cs
@OpenTibia.Client/Network/ClientConnection.cs
@OpenTibia.Client/Network/ClientPacketReader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement IncomingCommandHandler</name>
  <files>OpenTibia.Client/Network/IncomingCommandHandler.cs</files>
  <action>
    Create `IncomingCommandHandler`.
    It should have a method `public void HandlePacket(IIncomingPacket packet, ClientContext context)` that:
    1. Identifies the command type for the packet.
    2. Instantiates the command.
    3. Dispatches it using `context.ClientServer.Post(context, () => command.Execute())`.
  </action>
  <verify>
    <automated>ls OpenTibia.Client/Network/IncomingCommandHandler.cs</automated>
  </verify>
  <done>IncomingCommandHandler implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ClientServer to IncomingCommandHandler</name>
  <files>OpenTibia.Client/ClientServer.cs</files>
  <action>
    In `OpenTibia.Client/ClientServer.cs`:
    1. Add an `IncomingCommandHandler` field.
    2. In the `Connect` method, subscribe to `_connection.OnPayloadReceived`.
    3. In the event handler:
       - Use `ClientPacketReader.Read(reader)` to get the packet.
       - Use `IncomingCommandHandler.HandlePacket` to dispatch the command.
  </action>
  <verify>
    <automated>grep "OnPayloadReceived" OpenTibia.Client/ClientServer.cs</automated>
  </verify>
  <done>ClientServer wired to the command handler.</done>
</task>

</tasks>

<verification>
Build OpenTibia.Client.
</verification>

<success_criteria>
- Project compiles.
- Network received payloads are routed to commands.
</success_criteria>

<output>
After completion, create `.planning/phases/09-network-command-handlers/09-02-SUMMARY.md`
</output>
