---
phase: 02-asset-pipeline-graphics
plan: 02
type: execute
wave: 2
depends_on: ["02-asset-pipeline-graphics-01"]
files_modified: [OpenTibia.Client/Graphics/SpriteLoader.cs, OpenTibia.Client.Tests/SpriteLoaderTests.cs]
autonomous: true
requirements: [REUSE-04, REUSE-05, ASSET-01]

must_haves:
  truths:
    - "SpriteLoader.LoadSprite(id) returns a Promise<Texture2D>."
    - "Sprites are loaded and decoded on a background thread."
    - "Texture creation (GPU upload) occurs on the main thread via MainThreadDispatcher."
    - "BGRA pixel data from OpenTibia.FileFormats is converted to RGBA."
  artifacts:
    - path: "OpenTibia.Client/Graphics/SpriteLoader.cs"
      provides: "Asynchronous sprite loading"
  key_links:
    - from: "OpenTibia.Client/Graphics/SpriteLoader.cs"
      to: "OpenTibia.FileFormats.Spr.Sprite.Load"
      via: "Sprite decoding"
    - from: "OpenTibia.Client/Graphics/SpriteLoader.cs"
      to: "MainThreadDispatcher"
      via: "Texture creation"
---

<objective>
Implement the SpriteLoader to handle asynchronous decoding and loading of Tibia sprites from .spr files.

Purpose: Offloading expensive decoding and file I/O prevents frame stutters while maintaining smooth graphics loading.
Output: SpriteLoader class and integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-pipeline-graphics/02-RESEARCH.md
@.planning/phases/02-asset-pipeline-graphics/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SpriteLoader</name>
  <files>OpenTibia.Client/Graphics/SpriteLoader.cs</files>
  <action>
    Create `SpriteLoader` to handle sprite requests.
    - Constructor: Injected `MainThreadDispatcher`, `TextureCache`, and path to `.spr` file.
    - Method `LoadSprite(int spriteId)`: Returns `Promise<Texture2D>`.
    - Logic: Check `TextureCache` first. If missing, use `Task.Run` to load/decode in background.
    - Decoding: Use `OpenTibia.FileFormats.Spr.Sprite.Load` to decode RLE pixels.
    - Conversion: Convert the decoded pixels from BGRA to RGBA (swap R and B channels).
    - Dispatch: Use `MainThreadDispatcher.Post` to call `Raylib.LoadTextureFromImage` on the main thread.
    - Promise: Resolve promise once texture is loaded.
  </action>
  <verify>
    <automated>dotnet test OpenTibia.Client.Tests --filter SpriteLoaderTests</automated>
  </verify>
  <done>SpriteLoader correctly loads sprites asynchronously and resolves promises with valid textures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for SpriteLoader</name>
  <files>OpenTibia.Client.Tests/SpriteLoaderTests.cs</files>
  <behavior>
    - Test LoadSprite: Verifying `Promise` is returned and resolves eventually with non-zero Texture ID.
    - Test Channel Swap: Verifying color components are correctly mapped to RGBA (optional/requires pixel-level check).
  </behavior>
  <action>
    Create xUnit tests to verify asynchronous loading. Mock the MainThreadDispatcher to execute actions synchronously during test or wait for completion.
  </action>
  <verify>
    <automated>dotnet test OpenTibia.Client.Tests --filter SpriteLoaderTests</automated>
  </verify>
  <done>Integration tests prove async loading works and resolves the promise correctly.</done>
</task>

</tasks>

<verification>
1. SpriteLoader.LoadSprite does not block the caller.
2. Sprites are correctly decoded and converted to RGBA.
3. TextureCache is updated with the new texture.
</verification>

<success_criteria>
- SpriteLoader implemented with background processing.
- Promises resolve correctly with loaded textures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-pipeline-graphics/02-02-SUMMARY.md`
</output>
