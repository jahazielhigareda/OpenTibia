---
phase: 02-asset-pipeline-graphics
plan: 03
type: execute
wave: 2
depends_on: ["02-asset-pipeline-graphics-01"]
files_modified: [OpenTibia.Client/Graphics/CameraManager.cs, OpenTibia.Client/Program.cs]
autonomous: false
requirements: [ASSET-03]

must_haves:
  truths:
    - "Camera2D correctly zooms and pans based on input or world target."
    - "World coordinates (e.g., tile X, Y) map correctly to screen pixels (32px scale)."
    - "Program.cs loop calls MainThreadDispatcher.Execute()."
    - "Program.cs renders a grid and a test sprite using the Camera system."
  artifacts:
    - path: "OpenTibia.Client/Graphics/CameraManager.cs"
      provides: "Camera2D abstraction and coordinate mapping"
  key_links:
    - from: "OpenTibia.Client/Program.cs"
      to: "Camera2D"
      via: "Raylib.BeginMode2D"
---

<objective>
Implement the 2D camera system and integrate it into the game loop for world-to-screen coordinate mapping.

Purpose: Enable world traversal and consistent rendering scale for game objects.
Output: CameraManager class and updated Program.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-pipeline-graphics/02-RESEARCH.md
@.planning/phases/02-asset-pipeline-graphics/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CameraManager</name>
  <files>OpenTibia.Client/Graphics/CameraManager.cs</files>
  <action>
    Create a `CameraManager` class to encapsulate Raylib's `Camera2D`.
    - Hold a `Camera2D` instance.
    - Implement `Update()`: Handle basic panning (arrow keys) and zooming (mouse wheel) for testing.
    - Implement `GetWorldToScreen(int tileX, int tileY)`: Returns `Vector2` (tileX * 32, tileY * 32).
    - Provide access to `Camera2D` for `BeginMode2D`.
  </action>
  <verify>
    <automated>MISSING — Requires visual check in Program.cs</automated>
  </verify>
  <done>CameraManager correctly calculates world-to-screen positions and manages Camera2D state.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Camera and Dispatcher into Program.cs</name>
  <files>OpenTibia.Client/Program.cs</files>
  <action>
    Modify the game loop in `Program.cs`:
    - Instantiate `MainThreadDispatcher` and `CameraManager`.
    - Call `MainThreadDispatcher.Execute()` at the start of the `Update` part of the loop.
    - Call `CameraManager.Update()` before drawing.
    - Wrap the world rendering in `Raylib.BeginMode2D(camera.Camera2D)` and `Raylib.EndMode2D()`.
    - Draw a placeholder grid (e.g., 20x20 tiles) and a test sprite (ID 100) at world (0,0).
  </action>
  <verify>
    <automated>MISSING — Visual verification required</automated>
  </verify>
  <done>Game loop executes main-thread actions and uses the camera system for rendering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Asynchronous sprite loading and camera-based rendering.</what-built>
  <how-to-verify>
    - Run the application.
    - Verify that a grid is rendered.
    - Verify that you can pan (arrow keys) and zoom (mouse wheel).
    - If a `.spr` file is present at the configured path, verify that a sprite eventually appears at (0,0).
    - Check for console/log errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
1. Application renders a grid with 32px tiles.
2. Camera panning and zooming work as expected.
3. MainThreadDispatcher handles texture uploads from SpriteLoader correctly.
</verification>

<success_criteria>
- Camera system fully integrated.
- World-to-screen mapping verified visually.
- Async sprite loading verified visually.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-pipeline-graphics/02-03-SUMMARY.md`
</output>
