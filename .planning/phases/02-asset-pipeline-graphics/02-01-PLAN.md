---
phase: 02-asset-pipeline-graphics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client/Graphics/MainThreadDispatcher.cs, OpenTibia.Client/Graphics/TextureCache.cs, OpenTibia.Client.Tests/TextureCacheTests.cs]
autonomous: true
requirements: [ASSET-02]

must_haves:
  truths:
    - "MainThreadDispatcher executes actions on the main Raylib thread."
    - "TextureCache correctly reuses existing Raylib Texture2D instances."
    - "UnloadAll() in TextureCache disposes all stored GPU textures."
  artifacts:
    - path: "OpenTibia.Client/Graphics/MainThreadDispatcher.cs"
      provides: "Thread-safe main-loop synchronization"
    - path: "OpenTibia.Client/Graphics/TextureCache.cs"
      provides: "Raylib Texture2D lifecycle management"
  key_links:
    - from: "OpenTibia.Client/Graphics/TextureCache.cs"
      to: "Raylib.UnloadTexture"
      via: "Cleanup call"
---

<objective>
Build the core infrastructure for asynchronous asset management and main-thread synchronization.

Purpose: Raylib/OpenGL textures must be created on the main thread. We need a dispatcher to bridge background loaders with the main loop.
Output: MainThreadDispatcher and TextureCache with unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-pipeline-graphics/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MainThreadDispatcher</name>
  <files>OpenTibia.Client/Graphics/MainThreadDispatcher.cs</files>
  <action>
    Create a thread-safe `MainThreadDispatcher` class using a `ConcurrentQueue<Action>`.
    - `Post(Action action)`: Enqueues an action.
    - `Execute()`: Dequeues and runs all pending actions.
    This will be used to run Raylib texture creation from background threads.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create placeholder test first</automated>
  </verify>
  <done>MainThreadDispatcher correctly executes posted actions when Execute() is called.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TextureCache</name>
  <files>OpenTibia.Client/Graphics/TextureCache.cs</files>
  <action>
    Create `TextureCache` to manage `Texture2D` instances.
    - Use a `Dictionary<int, Texture2D>` for storage.
    - Implement `bool TryGet(int id, out Texture2D texture)`.
    - Implement `void Store(int id, Texture2D texture)`.
    - Implement `void Unload(int id)`: Calls `Raylib.UnloadTexture` and removes from cache.
    - Implement `void UnloadAll()`: Iterates through all textures, unloads them, and clears the dictionary.
  </action>
  <verify>
    <automated>dotnet test OpenTibia.Client.Tests --filter TextureCacheTests</automated>
  </verify>
  <done>TextureCache manages Raylib Texture2D lifecycle correctly and avoids redundant loads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for TextureCache</name>
  <files>OpenTibia.Client.Tests/TextureCacheTests.cs</files>
  <behavior>
    - Test Store and TryGet: Storing a texture and retrieving it by ID.
    - Test Unload: Unloading a specific texture removing it from cache.
    - Test UnloadAll: Clearing all textures from cache.
  </behavior>
  <action>
    Create xUnit tests to verify `TextureCache` behavior. Note: Since `Raylib.UnloadTexture` requires an active window/context, tests might need to mock Raylib or run in a headless-safe way (e.g., just verify dictionary state if Raylib calls are difficult to test in CI).
  </action>
  <verify>
    <automated>dotnet test OpenTibia.Client.Tests --filter TextureCacheTests</automated>
  </verify>
  <done>Tests pass and cover all core Cache functions.</done>
</task>

</tasks>

<verification>
1. `MainThreadDispatcher` executes actions posted from another thread.
2. `TextureCache` stores and retrieves textures.
3. `UnloadAll` clears the cache.
</verification>

<success_criteria>
- MainThreadDispatcher and TextureCache implemented.
- Unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-pipeline-graphics/02-01-SUMMARY.md`
</output>
