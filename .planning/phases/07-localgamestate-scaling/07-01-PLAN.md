---
phase: 07-localgamestate-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client/LocalGameState.cs]
autonomous: true
requirements: [CLN-05]

must_haves:
  truths:
    - "LocalGameState uses a Dictionary<Position, Tile> for the Map."
    - "Map lookup and insertion are updated to use the dictionary."
  artifacts:
    - path: "OpenTibia.Client/LocalGameState.cs"
      provides: "Scalable map representation"
---

<objective>
Transition LocalGameState from a fixed array map to a Dictionary for better scalability and support for sparse/non-contiguous data.

Purpose: Support large or sparse maps without excessive memory usage.
Output: Refactored LocalGameState.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Client/LocalGameState.cs
@OpenTibia.Common/Structures/Position.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor LocalGameState Map property</name>
  <files>OpenTibia.Client/LocalGameState.cs</files>
  <action>
    Modify `OpenTibia.Client/LocalGameState.cs`:
    1. Change `public Tile[] Map { get; set; }` to `public Dictionary<Position, Tile> Map { get; } = new Dictionary<Position, Tile>();`.
    2. Update the constructor to initialize a default tile if needed, but since it's now a dictionary, we can just add a tile at (0,0,0) as a placeholder.
    3. Add helper methods if necessary: `public Tile GetTile(Position pos)` and `public void SetTile(Position pos, Tile tile)`.
  </action>
  <verify>
    <automated>grep "Dictionary<Position, Tile>" OpenTibia.Client/LocalGameState.cs</automated>
  </verify>
  <done>LocalGameState refactored to use dictionary.</done>
</task>

</tasks>

<verification>
Build OpenTibia.Client and check for errors in other files.
</verification>

<success_criteria>
- Map is a Dictionary.
- LocalGameState compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/07-localgamestate-scaling/07-01-SUMMARY.md`
</output>
