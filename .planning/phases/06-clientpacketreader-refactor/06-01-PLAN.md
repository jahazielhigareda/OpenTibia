---
phase: 06-clientpacketreader-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [OpenTibia.Client/Network/ClientPacketReader.cs, OpenTibia.Client/Program.cs]
autonomous: true
requirements: [CLN-04]

must_haves:
  truths:
    - "ClientPacketReader uses a Dictionary for packet dispatch."
    - "Hardcoded switch statement is removed."
    - "New packets can be registered without modifying ClientPacketReader code."
  artifacts:
    - path: "OpenTibia.Client/Network/ClientPacketReader.cs"
      provides: "Extensible packet reader"
---

<objective>
Refactor ClientPacketReader to use a scalable dictionary-based registry for packet dispatch.

Purpose: Avoid monolithic switch statements and enable easier addition of new packets.
Output: Refactored ClientPacketReader.cs and updated registration in Program.cs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@OpenTibia.Client/Network/ClientPacketReader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ClientPacketReader</name>
  <files>OpenTibia.Client/Network/ClientPacketReader.cs</files>
  <action>
    Rewrite `ClientPacketReader` to:
    1. Have a static `Dictionary<byte, Type> _packetTypes = new Dictionary<byte, Type>();`.
    2. Add a static method `public static void Register(byte id, Type type)`.
    3. Update `Read` method to:
       - Get the type from dictionary using `packetId`.
       - If found, use `Activator.CreateInstance(type)` to create the packet.
       - Call `Read(reader, null)` on the instance.
       - Return the instance.
  </action>
  <verify>
    <automated>grep "Dictionary<byte, Type>" OpenTibia.Client/Network/ClientPacketReader.cs</automated>
  </verify>
  <done>ClientPacketReader refactored to use dictionary.</done>
</task>

<task type="auto">
  <name>Task 2: Register packets in Program.cs</name>
  <files>OpenTibia.Client/Program.cs</files>
  <action>
    In `OpenTibia.Client/Program.cs`, before the game loop starts, register the existing packets:
    - `ClientPacketReader.Register(0x0A, typeof(SelfAppearIncomingPacket));`
    - `ClientPacketReader.Register(0x64, typeof(MapDescriptionIncomingPacket));`
    - `ClientPacketReader.Register(0x6D, typeof(CreatureMoveIncomingPacket));`
  </action>
  <verify>
    <automated>grep "ClientPacketReader.Register" OpenTibia.Client/Program.cs</automated>
  </verify>
  <done>Packets registered at startup.</done>
</task>

</tasks>

<verification>
Build OpenTibia.Client and run unit tests.
</verification>

<success_criteria>
- ClientPacketReader is extensible.
- Unit tests (which might rely on packet reading) still pass.
- Project compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/06-clientpacketreader-refactor/06-01-SUMMARY.md`
</output>
